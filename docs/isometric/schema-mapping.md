# Requirement-to-Schema Mapping (Current Repo)

> Non-authoritative interpretation.
> Coverage reflects the current implemented schema/migration (`src/db/schema/*`, `drizzle/0000_blue_mastermind.sql`) and does not imply Isometric acceptance.

| Requirement ID/Reference | Current table.column(s) | Coverage | Gap notes | Priority |
|---|---|---|---|---|
| Feedstock SC evidence each RP (`biomass-feedstock-accounting` §2.1) | `feedstocks.feedstockTypeId`, `feedstocks.feedstockSourceRegion` (auto-populated from `suppliers.sourceRegion` via delivery) | missing | **`feedstock_sc_assessments` was dropped in migration `0037` and never re-created** — no structured SC-criterion assessment store remains, only ambient feedstock attributes. No canonical criterion catalog and no per-RP assessment guardrail. | P0 |
| Ineligible biomass >25% mass rule (module intro) | none stored — derived in `src/lib/chain-of-custody/sankey.ts` from lineage allocations flagged `feedstocks.eligibilityStatus = 'ineligible'` | partial | **2026-07-03 (ADR 0019):** the `total_feedstock_mass_kg`/`ineligible_feedstock_mass_kg` summary columns were dropped from `credit_batches` (migration `0067`) in favor of read-time derivation; the ineligible fraction is computed on read, clamped to never exceed the feedstock column it exits from. App-layer guardrail (>25% blocks issuance) still not implemented. | P0 |
| Counterfactual fate evidence + 10-year validity (§3.1) | `feedstocks.counterfactualCategory`, `feedstocks.counterfactualEmissions15Tons`, `feedstocks.counterfactualStorage50Tons`, `documents.*` | partial | Inputs exist, but no structured validity-cycle model (assessment date, expiry, reassessment trigger, sourcing-change invalidation). | P0 |
| Market leakage significance tests ML4/ML5/ML6 (§4.2) | `feedstocks.marketLeakageMethod`, `feedstocks.marketLeakageTons` | partial | Missing supplier-revenue evidence, disposal-cost benchmark fields, benchmark-priced revenue evidence, and test-outcome snapshots by assessment period. | P1 |
| Sampling plan in PDD (`R-S8K1-1`) | `documents.documentType='pdd'`, `production_processes.randomSamplingPlanRef` | partial | PDD evidence and the agreed random-plan reference can be recorded; no normalized sampling-plan version entity with declared cadence and justification fields yet. | P1 |
| Initial Method A minimum and Method B choice (`G-F74T-0`, `G-2W0F-0`) | `production_processes.{establishedAt,agreedBaselineSize,randomSamplingPlanRef,moisturePathway}`, `credit_batches.sampling`, `samples.{creditBatchId,samplingTime}` | partial | **ADR 0022:** each batch stores an immutable sampled/unsampled choice. Unsampled creation is Isometric-gated and uses a live eligible-sample count since the current process epoch, with all three prerequisites and a baseline floor of 30. No regime/unlock field or sample-floor DB trigger remains. The unconfirmed 200-year `_unsampled` measurement-sample POST remains code-gated. | P0 |
| Reactor design diagram (`R-6AQG-1`) | `reactors.specifications`, `documents.documentType='pdd'` | partial | No explicit design-diagram artifact linkage/version approval state. | P1 |
| Leakage verification and pressure monitoring (`R-SZK5-1`, `G-MJPG-1`) | `productionRunReadings.pressureBar`, `productionRunReadings.timestamp`, `documents.documentType='calibration_certificate'` | partial | Timeseries pressure exists; no explicit annual calibration schedule model or leak-test standard/result structure. | P0 |
| Loss accounting for process upsets (§8.4.2) | `incidentReports.*`, `creditBatches.totalCo2eEmissionsTons` | partial | Incident log exists, but no dedicated lost-mass adjustment ledger tied to delivery/application corrections and batch-level deductions. | P0 |
| Biochar eligibility chemistry (Soil §3.3 Table 2) | `samples.hToCOrgRatio`, `samples.oToCOrgRatio`, `samples.*MgKg`, `samples.pahTotalMgKg`, `samples.pcbTotalMgKg`, `samples.dioxinsNgKg`, `samples.furansNgKg` | full | Core analysis fields present; the H/C_org<0.5 & O/C_org<0.2 pass/fail is implemented (`evaluateRunEligibility`, judged on the **per-credit-batch pooled replicate mean** per D8 after the Tier-1 re-grain) and hard-blocks removal submission (`evaluateDurabilitySubmissionGates`, credit-batch-grained). | P0 |
| 200-year durability equation inputs (Soil §5.1.1.3.1 Eq.3) | `creditBatches.durabilityOption`, `creditBatches.hToCorgRatio`, `creditBatches.fDurableCalculated`, `certifierProjects.defaultSoilTemperatureC`, `certifierProjects.defaultSoilTemperatureSource`, `applications.soilTemperatureSource`, `applications.soilTemperatureC`, `soilTemperatureMeasurements.*` | partial | Inputs exist. **Tier-1 (2026-06-20):** per-batch H/C aggregation is credit-batch-grained (`buildPerBatchDurabilityData`, `durability-aggregation.ts`); soil temperature is now an operator-declared **facility reference** (`certifierProjects.defaultSoilTemperatureC` + `defaultSoilTemperatureSource`, 7 °C floor, `resolveFacilityReferenceSoilTemperature`), with the per-application `soilTemperatureC` repurposed as a future per-removal override. The measurement-samples step (`durability-measurement-samples.ts` → `transformers/measurement-sample.ts`) carries H/C + total/inorganic carbon + product mass + the soil reference so the registry computes F_durable itself (D1/D2), and a durability evidence-ledger PDF reconciles raw replicates → submitted mean+std-dev. DB migrations `0053`/`0054` guard verified/issued 200-year batches against missing or cleared soil-temperature evidence. **The 200-year POST remains code-gated pending H/C unit and binding confirmation.** | P0 |
| Soil temperature baseline quality (>=10/site-month, `G-QMBJ-0`) | `soilTemperatureMeasurements.measurementDate`, `soilTemperatureMeasurements.temperatureC`, `soilTemperatureMeasurements.measurementMethod` | partial | Measurement table exists; no automatic completeness check per site-month. | P0 |
| 1000-year durability inputs (Soil §5.1.1.3.2 Eq.6) | `samples.randomReflectanceR0Percent`, `samples.reactiveCarbonPercent`, `samples.residualCarbonPercent`, `creditBatches.meanRandomReflectancePercent`, `creditBatches.stdRandomReflectance`, `creditBatches.meanNonReactiveCarbonPercent`, `creditBatches.stdNonReactiveCarbonPercent` | partial | Core inputs exist; no explicit segregation/control model when combining 200-year and 1000-year pathways. **2026-07-03 (ADR 0013 amendment, issue #142):** `computeFDurable1000` (Eq.6, bounded) now consumes these inputs as a **local preview only**, feeding `buildCo2eStoredPreview`; the module's R₀-term normalization is internally ambiguous and unconfirmed by Isometric (see `open-questions.md`), so no live 1000-year registry submission exists yet. | P1 |
| Stockpiling controls (Soil §7.3) | `stockpile_events.started_at`, `stockpile_events.ended_at`, `stockpile_events.risk_level`, `stockpile_events.exception_ref`, `stockpile_events.last_control_at` | schema-done | New `stockpile_events` table added. DB check enforces `exception_ref` is required when duration exceeds 12 months. App-layer UI and duration alerts planned for next PR. | P0 |
| Point-of-mixing constraints (Soil §9.4.2) | `formulations.biocharRatio`, `biocharProducts.composition`, `creditBatches.mixingTimelineDays` | partial | Ratio and timeline are representable; no explicit irreversibility/fuel-unsuitability attestation model. | P0 |
| Application spreading proof (`G-BCH4-0`) | `applications.evidenceMethod`, `applications.gisBoundaryReference`, `documents.documentType in ('photo','pdf','weighbridge_ticket','affidavit')`, `documents.metadata.geotagStatus`, `documents.metadata.evidenceRole`, `documents.metadata.logbookEvidenceType`, `documents.capturedAt` | partial | Readiness requires visual applications to have geotagged stockpile, spreading, and incorporation photos. Boundary applications require a nonblank GIS boundary reference plus semantic logbook evidence typed as weighbridge, inventory, or affidavit. Upload accepts photos missing EXIF and records the gap in metadata. | P1 |
| Chain-of-custody handoff records (Soil §9.9) | none (`custody_handoffs` dropped in migration `0037`); custody reconstructed from the Chain-of-Custody trail (`documents.*`, transport legs, samples) | missing | **`custody_handoffs` was dropped in `0037` and never re-created** — no dedicated handoff ledger remains; custody is derived from the CoC trail rather than a canonical handoff entity. | P0 |
| Third-party purchaser affidavit (`G-SZZR-0`) | `creditBatches.affidavitReference`, `creditBatches.intendedUseConfirmation`, `creditBatches.companyVerificationRef`, `creditBatches.mixingTimelineDays`, `documents.documentType='affidavit'` | partial | Key references exist; no normalized affidavit table with required clause-level attestations and activity-evidence checks. | P1 |
| Facility electricity-intensity threshold >200 GWh (Energy §5.1) | `productionRuns.electricityKwh` | partial | Run-level electricity captured; no annual facility rollup/classification state. | P0 |
| Low-carbon power procurement EC1-EC5 (Energy §5.3) | `power_procurement_evidence.contract_type`, `power_procurement_evidence.generator_cod_date`, `power_procurement_evidence.grid_region`, `power_procurement_evidence.matching_type`, `power_procurement_evidence.eac_registry`, `power_procurement_evidence.eac_retirement_id`, `power_procurement_evidence.retired_at` | schema-done | New `power_procurement_evidence` table added, keyed to facility+period. Stores hard-to-derive regulatory facts; EC1-EC5 pass/fail derived by app logic. No boolean flags stored. | P0 |
| Meter quality and hourly reporting (Energy §5.6) | `productionRuns.electricityKwh`, `productionRunReadings.timestamp` | partial | No dedicated electricity meter stream and meter uncertainty metadata model. | P1 |
| Fuel emissions accounting Eq 7 (Energy §6) | `productionRuns.dieselOperationLiters`, `productionRuns.dieselGensetLiters`, `productionRuns.preprocessingFuelLiters` | partial | Combined diesel litres (genset + startup/preprocessing) submit as one `pyrolysis/fuel_usage_by_volume/volume_of_fuel` datapoint (issue #319; ADR 0015 amendment) — no litres→kWh conversion. The volumetric well-to-wheel diesel EF is a fixed input pre-bound on the Isometric template, so noma stores no EF locally. Wood / kg-metered fuels (`fuel_usage_by_mass`) deferred to #319b. | P0 |
| Transportation method hierarchy and leg-level quantification (Transport §3) | `transportLegs.calculationMethodType`, `transportLegs.fuelConsumedLiters`, `transportLegs.electricityKwh`, `transportLegs.distanceKm`, `transportLegs.loadMassKg`, `transportLegs.emissionFactorUsed`, `transportLegs.transportEmissionsCo2eKg` | partial | Quantification methods are modeled with conditional checks; no explicit evidence flag to justify fallback to distance method when energy method infeasible. | P1 |
| Round-trip assumption + onward-leg evidence (Transport §5) | `transportLegs.distanceKm`, `documents.entityType='transport_leg'` | partial | No explicit round-trip-accounted flag or onward-route evidence requirement field. | P1 |
| BCU retirement/additionality/no-double-counting (Transport §4.1 EC1-EC4) | none (BCU fields removed in Feb 2026 simplification) | missing | Requires dedicated BCU model if Book-and-Claim approach is adopted. | P1 |
| Net removal equation for RP (Biochar Eq.1) | none stored — CO2e stored derived on read via `buildCo2eStoredPreview`/`getCo2eStoredPreviews` (`src/data-access/credit-batch-previews.ts`); emissions/counterfactual are registry-owned (ADR 0018), noma passes explicit nulls | full | **2026-07-03 (ADR 0019):** `creditBatches.totalCo2eStoredTons`/`totalCo2eEmissionsTons`/`totalCo2eCounterfactualTons` were dropped (migration `0067`) — nothing kept them in sync with edited/deleted member applications. Net removal is still fully derivable, now computed fresh on every read instead of read from a column that could drift. | P0 |
| Reporting Period end = biochar application (Biochar §8.6.2) | `applications.application_date` → `certifier_removals.completed_on` / GhgEntry `completed_on` | partial | Issue #320: `completed_on` = MAX application date across the removal's lineages (fails closed without one; inversion vs production start blocked pre-POST). **Gap:** `started_on` still = earliest production start, not earliest feedstock sourcing (§8.6.2 "begins with biomass feedstock sourcing") — deferred follow-up. | P0 |
| Materiality threshold and reassessment cadence (GHG §5) | none (`ghg_materiality_assessments` dropped in migration `0037`) | missing | **`ghg_materiality_assessments` was dropped in `0037` and never re-created** — no assessment structure remains and no automatic `<1%` threshold guardrail at issuance (tracked as P0-13 in `open-questions.md`). | P0 |
| Co-product allocation (GHG §6.1 Procedure 4) | none explicit | missing | No dedicated co-product allocation model linking shared emissions and stored carbon allocation outcomes. | P1 |
| Emissions amortization review schedule (GHG §7) | none explicit | missing | No model for Y1/Y3/Y5/renewal review checkpoints. | P1 |
| Reversal risk and buffer reassessment | `creditBatches.bufferPoolPercent` | partial | Buffer % exists, but no reversal-risk questionnaire/score history and reassessment event log. | P0 |
| Monitoring-parameter inventory table in PDD (`R-ENZR-0`, `G-02XQ-0`) | `documents.documentType='pdd'` | missing | No structured monitoring-parameter register tied to selected protocol/module requirements. | P1 |
| Embodied-emissions source documentation | `documents.*` (the previously-listed `emissionFactors.*` table and `productionRuns.emissionFactorsUsed` column do not exist — dropped in `0037` / never added) | missing | No normalized embodied inventory (materials/equipment, EPD/LCA references, verification status, allocation cycle); only generic document attachments. | P0 |
