You are the terminal browser runner. Perform this audit directly with your own computer-use tools in this process. Do not invoke `codex`, delegate, spawn another agent/process, or reread/execute the `codex-computer-use` workflow. Use exactly ONE visible browser session.

You are performing a long, evidence-heavy staging QA audit using computer use in ONE serial visible browser session.

Repository: /Users/kenji/Dropbox/Maji/18 Dark Earth Carbon/noma-dmrv
Staging: https://staging.noma.maji.studio
Artifact directory: /Users/kenji/Dropbox/Maji/18 Dark Earth Carbon/noma-dmrv/docs/qa/artifacts/2026-07-18-staging-full-journey

Read ADMIN_EMAIL and ADMIN_PASSWORD from .env.local and enter them only into the visible login form. Never print, persist, screenshot, or report either value. Do not include personal names/emails in evidence. Redact incidental PII before saving. Use staging only and a facility visibly designated for QA/testing. If multiple qualify, choose the clearest and record only its stable ID or sanitized alias.

Hard safety rules: UI/browser only; no source inspection as substitute, no DB inspection, no direct HTTP/API calls, no code changes, no GitHub issues, no external submission, no non-QA record modification/deletion. Create records prefixed QA-20260718-. Do not guess if an action might affect real data. Observe console/network where computer-use supports it and sanitize relevant evidence. Take screenshots after important transitions/findings. Attempt a screen recording of the primary journey if supported; otherwise document the limitation. Stay in one visible browser session.

PASS 1: authenticate as current admin and visit these routes: /dashboard, /facilities, /reactors, /storage-locations, /suppliers, /suppliers/[supplierId], /customers, /customers/[customerId], /feedstocks, /formulations, /production-runs, /production-runs/[productionRunId], /biochar-products, /orders, /deliveries, /applications, /credit-batches, /credit-batches/[id], /samples, /energy, /chain-of-custody, /settings/organization, /certification, /certification/settings, /certification/production-processes, /certification/removals, /certification/removals/[removalId], /certification/removals/[removalId]/review, /certification/ghg-statements, /admin, /admin/organizations, /admin/emission-estimates, /admin/users. Use session-created QA records for dynamic routes if available later; initially test only safe invalid IDs such as an obviously synthetic non-enumerative ID if UI routing permits. Verify crashes/500, loading, empty guidance, redirects, permissions, admin role, registry gating. Do not capture PII on admin screens; crop/redact or omit screenshot and state why.

PASS 2: through visible UI create/reuse prerequisites in order: active QA facility; supplier + location; customer + application location; reactor; feedstock/storage and output bins; pyrolysis feedstock type; feedstock intake allocated to bin; production process; complete production run with valid start/end, wet mass, moisture, output, energy/readings when supported; biochar product; positive-stock-bounded order; delivery with dry<=wet mass; application with required evidence/soil temperature; credit batch for one feedstock/facility, matching run, <=1 month; at least 3 independent samples if feasible; removal; GHG statement. Prefer Quick-Add and verify it seeds invoking dropdown. For every created entity verify success, list presence, detail/panel, dependent dropdown, persistence/reload/date stability, facility context. Verify Chain of Custody DAG, Map, Sankey, lineage/split/trail. Keep pyrolysis vs blend distinct. Check H/C_org<0.5 and O/C_org<0.2. Certification must fail closed absent registry mapping. Removal gates must be actionable; GHG requires submitted eligible removals and production confirmation. Do not submit externally; if a submit would contact registry, stop at the visible gate and document.

PASS 3 on QA records only: empty/whitespace; oversized text; negative/zero/non-numeric/out-of-range; percentages outside 0..100; ratio/mass relationships; timezone-edge dates save/reload; duplicate/double submit; refresh/back mid-form; stale list/detail; safe attempted deletion of QA parent with QA children (stop before confirmation if ambiguity); facility switching; cross-facility deep links using ONLY IDs visibly authorized in this session. Verify recoverable validation, retained input, no duplicates, stable dates, integrity, stale reconciliation, safe scoping/canonicalization, consequential-action confirmation.

PASS 4: repeat the critical supplier -> intake -> run -> product -> order -> delivery -> application -> credit batch -> samples -> removal/GHG path with usability, tablet viewport, and keyboard lens. Assess next-step discoverability, terms/units, prerequisite explanations, facility context, validation specificity/retention, statuses, consequential actions, keyboard focus/labels, tablet usability. Capture meaningful friction.

Write these final reports directly in the artifact directory (do not expose credentials/PII): pass-1-cold-start.md, pass-2-complete-journey.md, pass-3-adversarial.md, pass-4-operator-ux.md, computer-use-summary.md. Each pass: steps, routes/role, observed vs expected, explicit pass/fail for every acceptance check, absolute screenshot path + sanitized caption for every key step/finding, exact repro for every problem, sanitized console/network evidence, blockers/uncertainty, UX friction. Summary: severity-ranked P0-P3 candidate findings; coverage checklist for EVERY route and chain entity; QA aliases/IDs only; removals/GHG final state; recording path/limitation; explicit untested/blocked checks; clearly distinguish visible observation from inference.

Be exhaustive but safe. If time/tool limits prevent full completion, prioritize truthful route coverage and primary chain, write partial reports with exact untested lists, and never claim unobserved behavior.
