# Evidence 27 — sanitized runtime and environment notes

- Final sampled staging browser warnings/errors: `0`.
- Final sampled sandbox browser warnings/errors: `0`.
- The connected browser did not expose a request ledger, so no raw HTTP method/path/status
  evidence was available.
- The connected Isometric MCP `how_to` tool was absent from the continuation tool inventory.
  No new direct Isometric MCP/API call was made. The initial run's successful `how_to`
  invocation remains documented in `00-known-good-inputs.md`.
- One native file-chooser attempt timed out because the Chrome extension did not have local
  file-URL access. No file bytes, local path, signed upload URL, or credential was sent or
  recorded.
- Browser automation finalized cleanly; the claimed staging and sandbox tabs were released.

This file intentionally contains counts and non-sensitive capability outcomes only.
